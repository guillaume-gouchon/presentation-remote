Presentation bootstrap
============

A presentation bootstrap project using Reveal.js, and including a remote controller web-app using WebRTC.

Does not work on iPhone as WebRTC is not (yet) supported.
